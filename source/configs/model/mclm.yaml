# @package _group_

name: "mclm"

desc_encoder: "source.encoder.MCLMEncoder.MCLMEncoder"

desc_encoder_hparams:
  architecture: "microsoft/codebert-base"
  output_attentions: False

code_encoder: "source.encoder.MCLMEncoder.MCLMEncoder"

code_encoder_hparams:
  architecture: "microsoft/codebert-base"
  output_attentions: False

lr: 1e-5
base_lr: 1e-7
max_lr: 1e-3

loss: "source.loss.TripletLoss.TripletLoss"
loss_hparams:
  name: "TripletLoss"

tokenizer:
  architecture: "microsoft/codebert-base"

predictions:
  path: "../resources/predictions/mclm_predictions.pt"