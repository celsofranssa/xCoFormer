# @package _group_

name: "lstm"

desc_encoder: "source.encoder.LSTMEncoder.LSTMEncoder"

desc_encoder_hparams:
  vocabulary_size: 50000
  representation_size: 768
  hidden_size: 768

code_encoder: "source.encoder.LSTMEncoder.LSTMEncoder"

code_encoder_hparams:
  vocabulary_size: 50000
  representation_size: 768
  hidden_size: 768

lr: 1e-5
base_lr: 1e-7
max_lr: 1e-3

loss: "source.loss.TripletLoss.TripletLoss"
loss_hparams:
  name: "TripletLoss"

tokenizer:
  architecture: "bert-base-uncased"

predictions:
  path: "../resources/predictions/lstm_predictions.pt"